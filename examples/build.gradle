plugins {
	id "org.jetbrains.kotlin.jvm" version "1.3.0" // no variables allowed in new plugins API
}

dependencies {
	compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
	compile project(':core')
}